import 'package:flutter/material.dart';

class WeekToDoPage extends StatelessWidget {
  const WeekToDoPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
